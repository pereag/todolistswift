version: 2.1

jobs:
  build:
    macos:
      xcode: 14.0.1
    steps:
      - checkout
      - run:
          name: fastlane scan
          command: |
            gem install fastlane
            fastlane scan --timeout 600

workflows:
  sample:
    jobs:
      - build
