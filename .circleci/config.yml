version: 2.1

jobs:
  build:
    macos:
      xcode: 14.0.1
    steps:
      - checkout
      - run: 
          name: Fastlane
          command: |
            fastlane scan

workflows:
  sample:
    jobs:
      - build
