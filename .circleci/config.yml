version: 2.1

jobs:
  build:
    macos:
      xcode: 14.0.1
    steps:
      - checkout
      - run: fastlane scan 

      #- store_test_results:
       #   path: test_output
      #- store_artifacts:
      #    path: test_output
       #   destination: scan-output

workflows:
  sample:
    jobs:
      - build
